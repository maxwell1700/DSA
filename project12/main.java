package project12;

public class main {
    public static void main(String[] args) {
        //binary trees
        //parent nodes have children
        //root has no parent node. top node
        //leaf have no children node
        // binary tree every node has at most 2 children (0, 1 or 2 children node)
         // - they have at most one root
        // only one way to get to each node. if there are more than one then its no longer binary
        //cyclic trees are not binary

    }
}
